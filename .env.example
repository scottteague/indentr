# Copy this file to .env and set ORGANIZ_DATA_DIR to the directory where
# you want PostgreSQL to store its data files.
#
# The path can be absolute or relative to the docker-compose.yml file.
# Relative paths are relative to the project root (where docker-compose.yml lives).
#
# Examples:
#   ORGANIZ_DATA_DIR=./data                       # project-local (default if unset)
#   ORGANIZ_DATA_DIR=/home/alice/organiz-pgdata    # absolute path
#   ORGANIZ_DATA_DIR=~/organiz-pgdata              # ~ expansion (podman-compose supports this)

ORGANIZ_DATA_DIR=./data
